{
    "name": "toneparse",
    "module": "index.ts",
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "private": true,
    "scripts": {
        "dev": "bun run --watch index.ts 'tests/assets/Arena Ready.patch'",
        "lint": "prettier -w .",
        "build": "tsc -p tsconfig.build.json"
    }
}
